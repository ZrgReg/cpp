// INCLUDE. --------------------------------------------------------------------
//------------------------------------------------------------------------------
// INCLUDE. --------------------------------------------------------------------
//------------------------------------------------------------------------------
#include "_srv/Srv/dbg.h"

#include "APlayer.h"


/* Player. ********************************************************************/
/******************************************************************************/

// Конструктор. ----------------------------------------------------------------
//------------------------------------------------------------------------------
APlayer::APlayer(QObject *parent) : QObject(parent) {
    player = new QMediaPlayer(this);

    // connect
//    connect(player, &QMediaPlayer::durationChanged
//            , this, &APlayer::durationChanged );
//    connect(player, &QMediaPlayer::positionChanged
//            , this, &APlayer::positionChanged );

}// APlayer

// Деструктор. -----------------------------------------------------------------
//------------------------------------------------------------------------------
APlayer::~APlayer() {

}//~APlayer

// Готовность плеера. ----------------------------------------------------------
//------------------------------------------------------------------------------
bool APlayer::isPlayerAvailable() const { return player->isAvailable(); }

// Выставить громкость. --------------------------------------------------------
//------------------------------------------------------------------------------
void APlayer::setVolume(int volume) { player->setVolume(volume); }

// Пуск проигрывания. ----------------------------------------------------------
//------------------------------------------------------------------------------
void APlayer::play(QUrl url) {
    FNC << R"(/ bgn)";

    player->setMedia(url);
    player->play();

    FNC << R"(\ end)";
}// play

// Скорость воспроизведения. ---------------------------------------------------
//------------------------------------------------------------------------------
void APlayer::tempo(qreal rate) { player->setPlaybackRate(rate); }

//------------------------------------------------------------------------------

